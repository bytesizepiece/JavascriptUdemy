const score1 = 40;
const score2 = 60;
const score3 = 80;

const total = score1 + score2 + score3;
const average = total / 3

console.log(total);
console.log(average);

const plates = 20
const people = 7

//calcs the remaining
let remainPlates = plates % people

//adds one to variable (increment)
remainPlates++

const message = "there are " + remainPlates + " plates available"

console.log(message);